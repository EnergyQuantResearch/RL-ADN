"""Packaged benchmark network CSV files used by RL-ADN.

The node CSV files contain bus identifiers and slack-bus metadata.
The line CSV files contain branch columns such as ``FROM``, ``TO``, ``R``,
``X``, ``B``, ``STATUS``, and ``TAP``.

The 25-, 69-, and 123-node benchmark cases originate from
https://github.com/DESL-EPFL/IEEE-benchmark-distribution-grids.
"""
